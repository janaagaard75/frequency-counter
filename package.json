{
  "name": "unique-characters",
  "version": "0.0.0",
  "description": "Crawl a website based on the sitemap and return a list of all unique characters used.",
  "repository": "https://github.com/janaagaard75/unique-characters.git",
  "author": "Jan Aagaard <jan@aagaard.net>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn run clean && webpack --mode production",
    "clean": "yarn install --frozen-lockfile && rimraf wwwroot && copyfiles --up 1 \"src/*.{html,png}\" wwwroot",
    "start": "yarn run clean && webpack-dev-server",
    "update-packages": "yarn upgrade && npx syncyarnlock -k -s && yarn install"
  },
  "dependencies": {},
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "ts-loader": "^8.1.0",
    "typescript": "^4.2.4",
    "webpack": "^5.31.2",
    "webpack-cli": "^4.6.0",
    "webpack-dev-server": "^3.11.2"
  }
}
